/**
 * constants.js — Shared constants across CardFlow frontend views.
 */

export const STATUS_TABS = ['All', 'Active', 'Inactive'];

export const DEFAULT_PAGE_SIZE_OPTIONS = [25, 50, 100];

export const CARD_STATUSES = {
  PENDING: 'pending',
  PRINTED: 'printed',
  REPRINT: 'reprint',
  REJECTED: 'rejected',
};
