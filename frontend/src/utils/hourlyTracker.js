/**
 * Hourly card delta tracking has been completely removed per project specification.
 * This file remains as a backward-compatible stub that returns 0.
 */

export function getHourlyChange() {
  return 0;
}
